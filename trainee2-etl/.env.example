# Same local Postgres from Trainee 1's docker-compose.
# Swap for the real staging DB URL once we get it.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/verifier_dashboard
REPORTS_OUTPUT_DIR=./reports-output
