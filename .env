# Point this at the real staging DB once we get the connection URL.
# Until then it targets the local docker-compose Postgres.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/verifier_dashboard
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=verifier_dashboard
